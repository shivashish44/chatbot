[phases.setup]
nixPkgs = ['python3', 'nodejs']

[phases.build]
nixPkgs = ['python3', 'nodejs']
cmds = [
  'pip install -r requirements.txt --no-cache-dir',
  'python -m compileall .'
]

[phases.collect-static-files]
paths = ['start.jpg','ping.jpg','*.py','utils/*']

[start]
cmd = "python app.py"
